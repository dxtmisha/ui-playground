# The prefix to the name by which we store data in the local storage
# Префикс к названию, по которому храним данные в локальном хранилище
PREFIX=__dUi

# The names of the designs connected in this project
# Названия дизайнов, подключенных в этом проекте
DESIGNS=m2,m3,c1,c2

# Main design system
# Главная система дизайна
DESIGNS_MAIN=m3
VITE_DESIGNS_MAIN=m3

# Variable names for global properties
# Названия переменных для глобальных свойств
DESIGNS_GLOBAL=UI
VITE_DESIGNS_GLOBAL=UI

# The separator in the names into tokens for grouped names, by which we split the names into parts
# Разделитель в названиях на токены для сгруппированных имен, по которым разделяем названия на части
TOKEN_SEPARATOR=/

# The base name, which is taken as the starting value. Used in grouped names
# Базовое название, которое принимается как стартовое значение. Используется в группированных именах
TOKEN_SEPARATOR_BASIC=basic

# Indicates how far inside the array it is necessary to check for the presence of grouped values
# Указывает, на сколько внутри массива надо проверять на наличие группированных значений
TOKEN_SEPARATOR_LIMIT=6

# Link to git with components
# Ссылка на git с компонентами
UI_GIT=git+https://github.com/dxtmisha/ui-playground.git
VITE_UI_GIT=git+https://github.com/dxtmisha/ui-playground.git

# Link to the website with all the icons and images
# Ссылка на сайт со всеми иконками и картинами
UI_WEB=https://ru.dev2.coralclub.app
VITE_UI_WEB=https://ru.dev2.coralclub.app

# Link to the location of the code by components
# Ссылка на расположение кода по компонентам
UI_PATH=/ui/
VITE_UI_PATH=/ui/

# Link to the location of the script for getting the translation text
# Ссылка на расположение скрипта для получения текста перевода
UI_API_TRANSLATE=restApi/uiTranslate
VITE_UI_API_TRANSLATE=restApi/uiTranslate
